# Core AI stack (stable versions for GitHub Actions CPU)
transformers==4.41.2
torch==2.3.1
accelerate==0.31.0
sentencepiece==0.2.0

# Fix NumPy incompatibility with torch/transformers
numpy<2

# HTML + parsing utilities
beautifulsoup4==4.12.3
lxml==5.2.2

# Optional but recommended (warnings + stability)
regex>=2023.12.25
safetensors>=0.4.3
